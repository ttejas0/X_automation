# google apis
secrets.json
# Node.js backend .gitignore

# Dependency directories
# Usually contains downloaded packages. Reinstall using 'npm install' or 'yarn install'.
# Never commit this directory.
node_modules/
jspm_packages/

# Logs
# Log files are often environment-specific or contain runtime information, not source code.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Diagnostic reports (generated by Node.js)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
# Temporary files, process IDs, lock files, etc.
*.pid
*.seed
*.pid.lock

# Build directories / Transpiled files
# Output from build tools (like Babel, TypeScript, Webpack, Parcel, etc.)
dist/
build/
out/
public/ # Often contains built frontend assets, adjust if your source files are here
.parcel-cache/
.svelte-kit/
.next/

# Test results and coverage reports
coverage/
.nyc_output/

# Environment variables
# Contains sensitive credentials and configuration specific to an environment.
# ALWAYS ignore these. Provide template/example files instead.
.env
.env.*
!.env.example
!.env.template
# Add any other specific env file names you use, e.g., .env.local

# Operating System generated files
# =================================

# macOS
.DS_Store
.AppleDouble
.LSOverride
# Thumbnails
._*
# Files that might appear on external disk
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.fuse_hidden*

# IDE / Editor directories
# ========================
# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains (WebStorm, IntelliJ IDEA, etc.)
.idea/

# Sublime Text
*.sublime-workspace
*.sublime-project

# Atom
.atom/

# Vim swap files
*.swp

# Other temporary/generated files
# ===============================
*.tmp

# Optional Files
# ==============

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional Prettier cache
.prettiercache

# Optional PM2 files
# ecosystem.config.js # Sometimes committed, sometimes ignored depending on team preference
pm2*.log # PM2 logs
.pm2/logs/

# TypeScript cache file
*.tsbuildinfo

# Yarn Integrity file (can sometimes cause conflicts in large teams)
.yarnclean

# Uploads directory (if your app handles user uploads directly to the filesystem)
# uploads/